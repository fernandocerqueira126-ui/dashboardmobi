---
phase: 03-lead-proposal-flow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/LeadsCRM.tsx
autonomous: true
requirements:
  - PROP-01
  - PROP-02
must_haves:
  truths:
    - "O botão 'Criar Proposta' (primário) aparece entre 'Editar/Mover' e 'Converter/Excluir'"
    - "O botão 'Ir para Atendimento' (outline com ícone MessageSquare) aparece logo após 'Criar Proposta'"
    - "Os botões 'Converter' e 'Excluir' permanecem exatamente no rodapé do modal sem alteração"
    - "Clicar em 'Criar Proposta' navega para /proposta/[leadId]"
    - "Clicar em 'Ir para Atendimento' navega para /atendimentos?phone=[lead.phone]"
  artifacts:
    - path: "src/pages/LeadsCRM.tsx"
      provides: "LeadDetailSheet com 2 novos botões de ação"
      contains: "navigate('/proposta/"
  key_links:
    - from: "LeadDetailSheet"
      to: "/proposta/[id]"
      via: "useNavigate hook"
      pattern: "navigate.*proposta"
    - from: "LeadDetailSheet"
      to: "/atendimentos?phone="
      via: "useNavigate hook"
      pattern: "navigate.*atendimentos.*phone"
---

<objective>
Adicionar dois novos botões de ação no `LeadDetailSheet` de `src/pages/LeadsCRM.tsx`.

Purpose: Criar pontos de entrada para o fluxo de proposta e de chat direto a partir do modal de detalhe do lead.
Output: LeadDetailSheet com 4 ações (Editar, Mover, **Criar Proposta**, **Ir para Atendimento**, Converter, Excluir).
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/pages/LeadsCRM.tsx

<interfaces>
<!-- Contratos necessários para o executor. Extraídos do codebase. -->

Da interface LeadDetailSheet (linhas 330-491 de LeadsCRM.tsx):
```typescript
// Props atuais do componente
interface LeadDetailSheetProps {
  lead: Lead | null;
  open: boolean;
  onOpenChange: (open: boolean) => void;
  onEdit: (lead: Lead) => void;
  onMove: (lead: Lead) => void;
  onConvert: (lead: Lead) => void;
  onDelete: (lead: Lead) => void;
}

// Bloco de ações atual (linhas 467-487):
// <div className="py-4 grid grid-cols-2 gap-2">
//   <Button variant="outline">Editar</Button>
//   <Button variant="outline">Mover</Button>
//   {lead.status !== "ganho" && <Button variant="outline" className="text-emerald-400">Converter</Button>}
//   <Button variant="outline" className="text-destructive">Excluir</Button>
// </div>
```

Do LeadsCRM principal (linhas 493-791):
```typescript
// useNavigate já está importado (linha 99)
const navigate = useNavigate(); // linha 495
```

Da interface Lead (src/contexts/LeadsContext.tsx):
```typescript
interface Lead {
  id: string;    // usar para /proposta/[id]
  phone: string; // usar para /atendimentos?phone=[phone]
  // ...outros campos
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar botões Criar Proposta e Ir para Atendimento ao LeadDetailSheet</name>
  <files>src/pages/LeadsCRM.tsx</files>
  <action>
    Modificar o componente `LeadDetailSheet` (definido internamente em LeadsCRM.tsx, linhas 330-491).

    **Passo 1: Atualizar o tipo de props** para adicionar `onGoToAtendimento` e `onCreateProposal`:
    ```typescript
    // ANTES (linha 330-346):
    function LeadDetailSheet({ lead, open, onOpenChange, onEdit, onMove, onConvert, onDelete })
    
    // DEPOIS — adicionar dois callbacks opcionais:
    function LeadDetailSheet({ lead, open, onOpenChange, onEdit, onMove, onConvert, onDelete, onCreateProposal, onGoToAtendimento }: {
      // ...props existentes...
      onCreateProposal?: (lead: Lead) => void;
      onGoToAtendimento?: (lead: Lead) => void;
    })
    ```

    **Passo 2: Reestruturar o bloco de ações** (linhas 467-487) mantendo os botões existentes mas inserindo os dois novos ENTRE o bloco Editar/Mover e o bloco Converter/Excluir.

    A nova estrutura deve ser:
    ```tsx
    {/* Actions */}
    <div className="py-4 space-y-2">
      {/* Linha 1: Editar + Mover */}
      <div className="grid grid-cols-2 gap-2">
        <Button variant="outline" size="sm" className="gap-1.5" onClick={() => { onOpenChange(false); onEdit(lead); }}>
          <Pencil className="w-3.5 h-3.5" />
          Editar
        </Button>
        <Button variant="outline" size="sm" className="gap-1.5" onClick={() => { onOpenChange(false); onMove(lead); }}>
          <ArrowRight className="w-3.5 h-3.5" />
          Mover
        </Button>
      </div>

      {/* Linha 2: Criar Proposta — botão primário, largura total */}
      <Button
        size="sm"
        className="w-full gap-1.5 btn-primary"
        onClick={() => { onOpenChange(false); onCreateProposal?.(lead); }}
      >
        <FileText className="w-3.5 h-3.5" />
        Criar Proposta
      </Button>

      {/* Linha 3: Ir para Atendimento — outline, largura total, ícone de chat */}
      <Button
        variant="outline"
        size="sm"
        className="w-full gap-1.5"
        onClick={() => { onOpenChange(false); onGoToAtendimento?.(lead); }}
      >
        <MessageCircle className="w-3.5 h-3.5" />
        Ir para Atendimento
      </Button>

      {/* Separador visual antes das ações destrutivas */}
      <Separator />

      {/* Linha 4: Converter + Excluir — sem alterar nada aqui */}
      <div className="grid grid-cols-2 gap-2">
        {lead.status !== "ganho" && (
          <Button variant="outline" size="sm" className="gap-1.5 text-emerald-400 border-emerald-500/30 hover:bg-emerald-500/10" onClick={() => { onOpenChange(false); onConvert(lead); }}>
            <UserCheck className="w-3.5 h-3.5" />
            Converter
          </Button>
        )}
        <Button variant="outline" size="sm" className="gap-1.5 text-destructive border-destructive/30 hover:bg-destructive/10" onClick={() => { onOpenChange(false); onDelete(lead); }}>
          <Trash2 className="w-3.5 h-3.5" />
          Excluir
        </Button>
      </div>
    </div>
    ```

    **Passo 3: Atualizar o uso do componente** no JSX principal (linha 782-790):
    ```tsx
    <LeadDetailSheet
      lead={selectedLead}
      open={isDetailOpen}
      onOpenChange={setIsDetailOpen}
      onEdit={handleEditLead}
      onMove={(l) => { setLeadToMove(l); setIsMoveDialogOpen(true); }}
      onConvert={(l) => { setLeadToConvert(l); setIsConvertDialogOpen(true); }}
      onDelete={(l) => { setLeadToDelete(l); setIsDeleteDialogOpen(true); }}
      onCreateProposal={(l) => navigate(`/proposta/${l.id}`)}
      onGoToAtendimento={(l) => navigate(`/atendimentos?phone=${encodeURIComponent(l.phone || '')}`)}
    />
    ```

    **Ícones já importados que PRECISAM estar presentes:** `FileText`, `MessageCircle` (ambos já importados nas linhas 83-84 de LeadsCRM.tsx). Não adicionar novas importações.

    **DESIGN RULE:** Não alterar nenhuma outra parte do modal. Os botões Converter e Excluir devem permanecer exatamente como estão funcionalmente.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Botão "Criar Proposta" (primário, width full) aparece no modal entre Mover e Converter
    - Botão "Ir para Atendimento" (outline, MessageCircle) aparece abaixo de Criar Proposta
    - Separador visual antes de Converter/Excluir
    - Converter e Excluir permanecem inalterados no rodapé
    - TypeScript compila sem erros na alteração feita
  </done>
</task>

</tasks>

<verification>
- Abrir um lead no Kanban e verificar visualmente o novo layout de botões
- Clicar em "Criar Proposta" → deve redirecionar para /proposta/[id] (retorna 404 por enquanto, ok)
- Clicar em "Ir para Atendimento" → deve redirecionar para /atendimentos?phone=[telefone]
- Verificar que Converter e Excluir continuam funcionando normalmente
</verification>

<success_criteria>
- LeadDetailSheet exibe 6 ações totais no layout: Editar | Mover / Criar Proposta / Ir para Atendimento / [Separador] / Converter | Excluir
- Navegação ocorre via useNavigate (sem reload de página)
- TypeScript sem erros de tipo
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-lead-proposal-flow/03-01-SUMMARY.md`
</output>
